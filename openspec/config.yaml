# OpenSpec / SDD Configuration
# Generated by sdd-init on 2026-05-21

project:
  name: electrician_capstone
  description: "Full-stack web app for a self-employed electrician. Built with React, Vite and Express. Featuring a responsive design, dynamic service management and admin dashboard."

stack:
  frontend:
    framework: react
    version: "^19.1.1"
    build: vite
    version_number: "^7.1.7"
  backend:
    framework: express
    version: "^4.18.2"
    database: mysql2
    version_number: "^3.15.1"

testing:
  strict_tdd: true
  test_runner: "npx vitest"
  note: "Vitest installed. /sdd-apply follows RED → GREEN → REFACTOR cycle."
  suggested_backend_runner: "npx jest"

phases:
  proposal_required: true
  spec_required: true
  design_required: true
  tasks_required: true

changelog:
  dir: openspec/changes
  archive_dir: openspec/changes/archive

specs:
  dir: openspec/specs
