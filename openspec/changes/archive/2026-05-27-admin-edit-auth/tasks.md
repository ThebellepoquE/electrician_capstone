# Tasks: Admin Edit + Auth Token

**Change:** `admin-edit-auth`

1. Add `modoEdicion`, `editandoId` state + `getAuthHeaders()` helper
2. Add auth headers to `agregarServicio` and `eliminarServicio` fetch calls
3. Add `iniciarEdicion()`, `cancelarEdicion()`, `actualizarServicio()` functions
4. Update form JSX: dynamic heading, submit button, cancel button
5. Add "Editar" button per service card
6. Build + lint verify
